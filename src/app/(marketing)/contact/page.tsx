import type { Metadata } from "next";
import Link from "next/link";
import { getPublicBuildsMode } from "@/lib/public-builds/constants";
import { PublicPage } from "../components/public-page";
import { ContactForm } from "./contact-form";
export const metadata: Metadata = { title: "Contact — Trajectas", description: "Talk to Trajectas about role-based assessment, your organisation or a partnership.", alternates: { canonical: "/contact" } };
export default async function Page({ searchParams }: { searchParams: Promise<{ topic?: string }> }) { const { topic } = await searchParams; return <PublicPage label="Contact" title="Tell us what you’re working on." intro="Whether you have a role in mind, a programme to plan or a question about Trajectas, start the conversation here."><div className="px-container px-contact-layout"><ContactForm initialTopic={topic} /><aside className="rb-aside"><h2>A useful place to start</h2><p>Tell us about the roles, people and decisions involved. You don’t need a finished brief.</p><h3>Prefer email?</h3><a className="px-link" href="mailto:hello@trajectas.com">hello@trajectas.com</a><p>We’ll use the contact details you provide to respond to your enquiry. Please don’t include candidate records or sensitive personal information.</p><h3>Just exploring?</h3><p>The Role Builder lets you try a one-person assessment. {getPublicBuildsMode() === "closed" && "An invitation is required during preview."}</p><Link className="px-link" href="/role-builder">Explore Role Builder</Link></aside></div></PublicPage>; }
