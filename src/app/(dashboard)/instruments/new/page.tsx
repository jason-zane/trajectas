import { CreateInstrumentForm } from './create-instrument-form'

export const metadata = {
  title: 'New instrument'
}

export default function NewInstrumentPage() {
  return <CreateInstrumentForm />
}
